package dj.song.mixer.demoapp.model
// data class for required fields
data class DomainUser(val id: Int,
                      val name: String,
                      val email: String,
                      val city: String)
